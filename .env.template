# Rempli automatiquement par `dev-auth-fetcher connect` (ou le skill auth-user-frontend).
# Le préfixe VITE_ est conservé pour rester compatible avec les noms de variables
# attendus par dev-auth-fetcher — ce projet n'utilise pas Vite.
# Copier ce fichier en .env (jamais versionné) avant de lancer `yarn dev:<module>`.
VITE_RECETTE=
VITE_XSRF_TOKEN=
VITE_ONE_SESSION_ID=

# Optionnel : app id Screeb de test pour le dev local (mock de /<module>/conf/public)
SCREEB_APP_ID_DEV=
